@using SandwichSwap.ViewModels
@model CreateSandwichViewModel

@for (int i = 0; i < Model.Rows; i++)
{
    <select id="toppingList">
        @foreach (Topping t in Model.Toppings)
        {
            <option value=@t.Id>@t.name</option>
        }
    </select><br />
}
